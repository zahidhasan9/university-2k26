import { FacultyAllocationForm } from "@/components/faculty-allocation-form"
import { AcademicFormShell } from "@/components/academic-form-shell"
import { API_ENDPOINTS, withQuery } from "@/lib/api-endpoints"
import { authenticatedRequest } from "@/lib/auth"
type Item = { _id: string; code?: string; title?: string; name?: string; academicBatch?: { _id: string } | string; user?: { firstName: string; lastName: string }; employeeId?: string }
export default async function Page() { const get = <T,>(path: string) => authenticatedRequest<{ items: T[] }>(path); const [teachers, courses, semesters, batches, sections] = await Promise.all([get<Item>(withQuery(API_ENDPOINTS.teachers.list, { status: "active", limit: 200 })), get<Item>(withQuery(API_ENDPOINTS.academics.courses, { status: "active", limit: 500 })), get<Item>(withQuery(API_ENDPOINTS.academics.semesters, { limit: 200 })), get<Item>(withQuery(API_ENDPOINTS.academics.batches, { status: "active", limit: 200 })), get<Item>(withQuery(API_ENDPOINTS.academics.sections, { status: "active", limit: 500 }))]); return <AcademicFormShell title="Allocate faculty course" description="Assign a curriculum course, batch, and section to a faculty member." backHref="/dashboard/faculty?tab=allocations"><FacultyAllocationForm teachers={teachers.data.items} courses={courses.data.items} semesters={semesters.data.items} batches={batches.data.items} sections={sections.data.items} /></AcademicFormShell> }
