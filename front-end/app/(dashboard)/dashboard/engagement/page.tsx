import Link from "next/link"
import { BriefcaseBusiness, MessageSquareWarning, Plus, UsersRound } from "lucide-react"
import { Badge } from "@/components/ui/badge"
import { Button } from "@/components/ui/button"
import { Card,CardContent,CardHeader,CardTitle } from "@/components/ui/card"
import { authenticatedRequest } from "@/lib/auth"
type Complaint={_id:string;ticketNumber:string;category:string;subject:string;description:string;priority:string;status:string;assignedTo?:{firstName:string;lastName:string};resolution?:string;createdAt:string}
type Alumni={_id:string;user:{firstName:string;lastName:string};program?:{name:string;code:string};graduationYear:number;currentOrganization?:string;jobTitle?:string;location?:string;bio?:string;status:string}
export default async function Page(){let complaints:Complaint[]=[],alumni:Alumni[]=[],error="";try{const d=await Promise.all([authenticatedRequest<{complaints:Complaint[]}>("/engagement/complaints/mine"),authenticatedRequest<{alumni:Alumni[]}>("/engagement/alumni")]);complaints=d[0].data.complaints;alumni=d[1].data.alumni}catch(c){error=c instanceof Error?c.message:"Engagement data unavailable"}return <div className="mx-auto max-w-[1500px] space-y-6"><div className="flex flex-col justify-between gap-4 sm:flex-row sm:items-end"><div><p className="text-sm font-medium text-primary">Campus community</p><h1 className="mt-1 text-3xl font-bold">Student engagement</h1><p className="mt-1 text-sm text-muted-foreground">Service complaints and the verified alumni network.</p></div><div className="flex gap-2"><Button render={<Link href="/dashboard/engagement/complaints/new"/>}><Plus/>Submit complaint</Button><Button variant="outline" render={<Link href="/dashboard/engagement/alumni/register"/>}><UsersRound/>Alumni profile</Button></div></div>{error&&<p className="rounded-xl bg-destructive/10 p-4 text-destructive">{error}</p>}<div className="grid gap-4 sm:grid-cols-3"><Metric icon={<MessageSquareWarning/>} value={complaints.length} label="Your complaints"/><Metric icon={<UsersRound/>} value={alumni.length} label="Alumni directory"/><Metric icon={<BriefcaseBusiness/>} value={alumni.filter(x=>x.currentOrganization).length} label="Career profiles"/></div><div className="grid gap-6 lg:grid-cols-2"><Card><CardHeader><CardTitle>Complaint tracker</CardTitle></CardHeader><CardContent className="space-y-3">{complaints.length===0&&<Empty text="You have not submitted any complaints."/>}{complaints.map(x=><div key={x._id} className="rounded-xl border p-4"><div className="flex justify-between gap-3"><div><p className="font-semibold">{x.subject}</p><p className="font-mono text-xs text-muted-foreground">{x.ticketNumber}</p></div><div className="flex gap-1"><Badge variant={x.priority==="urgent"?"destructive":"outline"}>{x.priority}</Badge><Badge variant="secondary">{x.status}</Badge></div></div><p className="mt-3 text-sm text-muted-foreground">{x.description}</p>{x.resolution&&<p className="mt-3 rounded-lg bg-muted p-3 text-sm"><strong>Resolution:</strong> {x.resolution}</p>}</div>)}</CardContent></Card><Card><CardHeader><CardTitle>Alumni directory</CardTitle></CardHeader><CardContent className="space-y-3">{alumni.length===0&&<Empty text="No public alumni profiles yet."/>}{alumni.map(x=><div key={x._id} className="rounded-xl border p-4"><div className="flex justify-between"><div><p className="font-semibold">{x.user?.firstName} {x.user?.lastName}</p><p className="text-xs text-muted-foreground">{x.program?.name} · Class of {x.graduationYear}</p></div><Badge variant="outline">{x.status}</Badge></div><p className="mt-3 text-sm">{x.jobTitle}{x.jobTitle&&x.currentOrganization?" at ":""}{x.currentOrganization}</p><p className="text-xs text-muted-foreground">{x.location}</p></div>)}</CardContent></Card></div></div>}
function Metric({icon,value,label}:{icon:React.ReactNode;value:number;label:string}){return <Card><CardContent className="p-5"><div className="text-primary [&_svg]:size-5">{icon}</div><p className="mt-4 text-2xl font-bold">{value}</p><p className="text-sm text-muted-foreground">{label}</p></CardContent></Card>}
function Empty({text}:{text:string}){return <p className="rounded-xl border border-dashed p-8 text-center text-sm text-muted-foreground">{text}</p>}
